.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install badger

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv badger
    $ pip install badger
